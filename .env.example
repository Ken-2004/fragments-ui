# Public browser configuration, embedded by Parcel at build time.
# Supply your own Cognito identifiers locally; never add AWS credentials or a client secret.
API_URL=http://localhost:8080
AWS_COGNITO_POOL_ID=
AWS_COGNITO_CLIENT_ID=
OAUTH_SIGN_IN_REDIRECT_URL=http://localhost:1234
